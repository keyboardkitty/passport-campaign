# Server
PORT=3000

# Admin (change on first use!)
ADMIN_USER=evan
ADMIN_PASS=changeme123

# SMTP (optional - leave blank to use simulated email)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=

# Secret for admin token
TOKEN_SECRET=change-this-to-a-random-string-abc123
